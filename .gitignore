.~lock.*
*.tmp
.DS_Store
Thumbs.db
*~
*.swp

# Local Claude Code session state created when opening this repo in Claude Code.
# Distinct from the plugin's .claude-plugin/ (which IS committed).
.claude/

